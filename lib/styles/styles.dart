import 'package:flutter/material.dart';

const textColor = Colors.white;
const cardColor = Colors.white;

const backgroundColor = Colors.blue;

const titleTextStyle = TextStyle(fontSize: 24.0, fontWeight: FontWeight.bold, color: textColor, shadows: [cardShadow]);
const subtitleTextStyle = TextStyle(fontSize: 16.0, color: textColor, shadows: [cardShadow]);
const subtitleBoldTextStyle = TextStyle(fontSize: 16.0, color: textColor, fontWeight: FontWeight.bold, shadows: [cardShadow]);

const double iconScale = 0.6;
const tempTextStyle = TextStyle(fontSize: 32.0, fontWeight: FontWeight.bold, color: textColor, shadows: [cardShadow]);

const cardBackColor = Color.fromARGB(64, 0, 0, 255);
const cardShadow = Shadow(color: Colors.black87, offset: Offset(1.0, 1.0), blurRadius: 10.0);
const dwCardText = TextStyle(fontSize: 20, color: cardColor, shadows: [cardShadow]);
const tmCardText = TextStyle(fontSize: 14, color: cardColor, shadows: [cardShadow]);